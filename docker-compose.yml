version: '3.2'

services:
  build-cache:
    build:
      context: .
      dockerfile: ./build-cache.Dockerfile
    image: maxmcd/deno:_build-cache
  deno:
    build: 
      context: .
      dockerfile: ./Dockerfile
    image: maxmcd/deno2:master
  slim:
    build:
      context: .
      dockerfile: ./slim.Dockerfile
    image: maxmcd/deno:slim
